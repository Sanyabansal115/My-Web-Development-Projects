{
  "name": "deploy",
  "version": "1.0.0",
  "description": "Deployment configuration for Sanya Bansal's Portfolio",
  "scripts": {
    "deploy:vercel": "vercel --prod",
    "deploy:netlify": "netlify deploy --prod --dir=dist",
    "deploy:gh-pages": "gh-pages -d dist",
    "build:deploy": "npm run build && npm run deploy:vercel"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Sanyabansal115/My-Web-Development.git"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0"
  }
}